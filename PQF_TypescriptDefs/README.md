# Typescript Definition for PQForce Backend Javascript API

## Build

1. Install dependencies: `npm install`
2. Build generator script: `npm run build`
3. Generate definition file: `npm run generate`
